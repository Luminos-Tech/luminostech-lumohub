# Important Commands

- `docker compose up --build`
- `docker compose -f docker-compose.dev.yml up --build`
- `python -m compileall -q backend/app`
- `npm ci`, `npm run lint`, `npm run build` from `frontend-web`
- Build/flash firmware with ESP-IDF from `Lumo/Lumo-LuminosTech`
