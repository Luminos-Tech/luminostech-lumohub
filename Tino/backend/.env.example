# Copy this file to `.env` and adjust as needed.

APP_DEBUG=false

# PostgreSQL — defaults match the local `lumohub_db` container
DB_HOST=localhost
DB_PORT=5432
DB_USER=lumohub
DB_PASSWORD=lumohub123
DB_NAME=lumohub_db
