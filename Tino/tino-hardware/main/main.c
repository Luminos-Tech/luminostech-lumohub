// app_main — entry point for the Tino hardware firmware (ESP-IDF).
//
// Intentionally empty: real application logic is added after the product
// description in `../docs/` is finalised. Once a meaningful app exists,
// `idf.py build` will produce a usable firmware image.

#include <stdio.h>

void app_main(void)
{
    // Placeholder. Real logic (Wi-Fi, sensors, networking, ...) goes here.
    (void)0;
}
